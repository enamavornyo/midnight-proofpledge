# Browser DApp network. Supported values: preview, preprod.
VITE_NETWORK_ID=preview

# Browser logging level used by the Midnight client libraries.
VITE_LOGGING_LEVEL=info

# Encryption password for CLI private-state storage. Set this in the shell before running the CLI.
PROOFPLEDGE_PRIVATE_STORAGE_PASSWORD=change-this-local-password
